import { createUseStyles } from "react-jss";

export const useMainPageStyles = createUseStyles({
    root: {
        width: "100%",
        height: "100%",
        display: 'flex',
        position: "relative",
        margin: 0,
        padding: 0,
    }
})